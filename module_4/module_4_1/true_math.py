import math
def divide(first, second):
    if second != 0:
        resalt = first / second
        return resalt
    else:
        from math import inf
        resalt = math.inf
        return resalt
        #print(float("inf"))

